mqtt :
  port : 1833
  ssl : false
  session_thread_count : 8
  broker :
    id : 1
    ssl : false
    password_must : false
    cluster_enabled: false

logging:
  config: classpath:config/log4j2.xml